/**
 * Atualmente, é cada vez mais comum utilizarmos libs que possuem a tipagem 
 * embutida no próprio pacote (por exemplo uma pasta `types` com um arquivo `index.d.ts`), 
 * como é o caso do **Knex** . Porém, ainda existem diversas libs que não possuem 
 * tipagem embutidas no próprio pacote (como é o caso do **React**). 
 * Para muitos desses casos, temos um projeto bem legal conhecido 
 * como **DefinitelyTyped** que fornece a tipagem correta da lib 
 * (é ele que fornece os famosos pacotes do npm `@types`). 
 * Segue abaixo o link do repositório do projeto no Github:
 * 
 * https://github.com/DefinitelyTyped/DefinitelyTyped
 */

 // Definitely Typed


 // https://www.typescriptlang.org/docs/handbook/declaration-files/templates/module-d-ts.html
 // https://www.typescriptlang.org/docs/handbook/declaration-files/introduction.html
